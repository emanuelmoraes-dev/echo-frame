windows:
	cd echo-frame-engine && make windows
linux:
	cd echo-frame-engine && make linux
macX86:
	cd echo-frame-engine && make macX86
macARM64:
	cd echo-frame-engine && make macARM64
wasm:
	cd echo-frame-engine && make wasm
